pipeline {  
    agent any  
    stages {  
            stage ('Git-Checkout') {  
                steps{
                    git url:'https://github.com/aka034/CI-CD-Pipeline.git', branch: 'master'
                    echo "Checkout successful";
                } 
            }
			}
			}

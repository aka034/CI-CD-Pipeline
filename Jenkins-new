pipeline {  
    agent any  
    stages {  
            stage ('Git-Checkout') {  
                steps{
                    git url:'https://github.com/aka034/MavenWebProject.git', branch: 'master'
                    echo "Checkout successful";
                } 
            }
			}
			}
